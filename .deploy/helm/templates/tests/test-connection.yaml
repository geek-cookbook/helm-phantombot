apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "PhantomBot.fullname" . }}-test-connection"
  labels:
    {{- include "PhantomBot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "PhantomBot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
