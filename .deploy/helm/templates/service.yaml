apiVersion: v1
kind: Service
metadata:
  name: {{ include "PhantomBot.fullname" . }}
  labels:
    {{- include "PhantomBot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "PhantomBot.selectorLabels" . | nindent 4 }}
